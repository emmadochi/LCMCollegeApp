// Firebase is deprecated and removed from this project.
// All authentication and course details are handled by the local PHP REST API and custom JWT guard.
export const auth = null;
export const db = null;
export default null;
